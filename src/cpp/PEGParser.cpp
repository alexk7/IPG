// This file was automatically generated by IPG on Fri Jul 22 18:15:33 2011
// (from /Users/alexk7/ipg/build/xcode/../../src/peg/peg.peg)
// DO NOT EDIT!
#ifndef BOOTSTRAP_TEST
#include <cassert>
#include <stdexcept>
#include <ostream>
#include <sstream>
#include <iomanip>
#include <algorithm>
#include <boost/format.hpp>
#endif

#ifndef BOOTSTRAP_TEST
#include <vector>
#include <bitset>
#include <boost/shared_ptr.hpp>
#include <tr1/unordered_map>
#include <tr1/unordered_set>
#endif

namespace PEGParser
{
	enum SymbolType
	{
		SymbolType_AND = 0,
		SymbolType_CLOSE = 1,
		SymbolType_Char = 2,
		SymbolType_Class = 3,
		SymbolType_Comment = 4,
		SymbolType_DOT = 5,
		SymbolType_Definition = 6,
		SymbolType_EndOfFile = 7,
		SymbolType_EndOfLine = 8,
		SymbolType_Expression = 9,
		SymbolType_Grammar = 10,
		SymbolType_Identifier = 11,
		SymbolType_LEFTARROW = 12,
		SymbolType_Literal = 13,
		SymbolType_NOT = 14,
		SymbolType_OPEN = 15,
		SymbolType_PLUS = 16,
		SymbolType_Prefix = 17,
		SymbolType_Primary = 18,
		SymbolType_QUESTION = 19,
		SymbolType_Range = 20,
		SymbolType_SLASH = 21,
		SymbolType_STAR = 22,
		SymbolType_Sequence = 23,
		SymbolType_Space = 24,
		SymbolType_Spacing = 25,
		SymbolType_Suffix = 26,
		SymbolTypeCount
	};

	typedef std::pair<SymbolType, const char*> Symbol;
	typedef std::vector<Symbol> Symbols;
	typedef std::tr1::unordered_map<const char*, const char*> EndMap;
	typedef std::tr1::unordered_set<const char*> FailSet;
	

	const char* SymbolName(SymbolType _type);

	class Parser
	{
	public:
		bool Parse(SymbolType _type, const char*& _p, bool _memoize = true);
		bool Traverse(SymbolType _type, const char*& _p, Symbols& _children, bool _memoize = true);
		void Print(std::ostream& _os, SymbolType _type, const char* _pNode, int _tabs = 0, int _maxLineSize = 100);

	private:
		bool Visit(SymbolType _type, const char*& _p, Symbols& _v);
		EndMap end[SymbolTypeCount];
		FailSet fail[SymbolTypeCount];
	};

	class Iterator
	{
	public:
		Iterator(SymbolType _type, const char* _pNode = 0);
		Iterator(const Iterator& _iOther);
		operator bool() const;
		Iterator& operator++();
		SymbolType GetType() const;
		const char* Begin() const;
		const char* End() const;
		Iterator GetChild(SymbolType _childT) const;
		Iterator GetNext(SymbolType _childT) const;
		void Print(std::ostream& _os, int _tabs = 0, int _maxLineSize = 100);

	private:
		Iterator(boost::shared_ptr<Parser> _pParser, boost::shared_ptr<Symbols> _pSiblings, SymbolType _childType);
		Iterator(const Iterator& _iOther, SymbolType _childType);
		void GoToNext(SymbolType _childType);
		void SkipSiblingsWithWrongType(SymbolType _childType);
		boost::shared_ptr<Symbols> GetChildren() const;

		SymbolType mType;
		const char* mpNode;
		boost::shared_ptr<Symbols> mpSiblings;
		Symbols::iterator miCurrent;
		mutable boost::shared_ptr<Symbols> mpChildren;
		boost::shared_ptr<Parser> mpParser;
	};

	std::ostream& operator<<(std::ostream& _os, const Iterator& _i);
}

namespace
{
	struct EscapeChar
	{
		EscapeChar(char _c);
		char c;
	};

	EscapeChar::EscapeChar(char _c) : c(_c)
	{
	}

	std::ostream& operator<<(std::ostream& _os, EscapeChar _e)
	{
		char c = _e.c;
		switch (c)
		{
			case '\\': c = '\\'; break;
			case '\n': c = 'n';  break;
			case '\r': c = 'r';  break;
			case '\t': c = 't';  break;
			case '\'': c = '\''; break;
			case '\"': c = '\"'; break;
			default:
				_os.put(c);
				return _os;
		}
		_os.put('\\');
		_os.put(c);
		return _os;
	}
}

const char* PEGParser::SymbolName(PEGParser::SymbolType _type)
{
	switch (_type)
	{
		case SymbolType_AND: return "AND";
		case SymbolType_CLOSE: return "CLOSE";
		case SymbolType_Char: return "Char";
		case SymbolType_Class: return "Class";
		case SymbolType_Comment: return "Comment";
		case SymbolType_DOT: return "DOT";
		case SymbolType_Definition: return "Definition";
		case SymbolType_EndOfFile: return "EndOfFile";
		case SymbolType_EndOfLine: return "EndOfLine";
		case SymbolType_Expression: return "Expression";
		case SymbolType_Grammar: return "Grammar";
		case SymbolType_Identifier: return "Identifier";
		case SymbolType_LEFTARROW: return "LEFTARROW";
		case SymbolType_Literal: return "Literal";
		case SymbolType_NOT: return "NOT";
		case SymbolType_OPEN: return "OPEN";
		case SymbolType_PLUS: return "PLUS";
		case SymbolType_Prefix: return "Prefix";
		case SymbolType_Primary: return "Primary";
		case SymbolType_QUESTION: return "QUESTION";
		case SymbolType_Range: return "Range";
		case SymbolType_SLASH: return "SLASH";
		case SymbolType_STAR: return "STAR";
		case SymbolType_Sequence: return "Sequence";
		case SymbolType_Space: return "Space";
		case SymbolType_Spacing: return "Spacing";
		case SymbolType_Suffix: return "Suffix";
		default: throw std::runtime_error(str(boost::format("Invalid Symbol Type: %1%") % _type));
	}
}

bool PEGParser::Parser::Parse(SymbolType _type, const char*& p, bool _memoize)
{
	const char* pBegin = p;
	bool r = true;
	char c;
	switch (_type)
	{
		case SymbolType_AND:
		{
			if (fail[SymbolType_AND].count(pBegin))
				return false;
			EndMap::iterator i = end[SymbolType_AND].find(pBegin);
			if (i != end[SymbolType_AND].end())
			{
				p = i->second;
				return true;
			}
			c = *p++;
			r = (c == '&');
			if (r)
			{
				r = Parse(SymbolType_Spacing, p, 0);
			}
			if (_memoize)
			{
				if (r)
					end[SymbolType_AND][pBegin] = p;
				else
					fail[SymbolType_AND].insert(pBegin);
			}
			return r;
		}

		case SymbolType_CLOSE:
		{
			if (fail[SymbolType_CLOSE].count(pBegin))
				return false;
			EndMap::iterator i = end[SymbolType_CLOSE].find(pBegin);
			if (i != end[SymbolType_CLOSE].end())
			{
				p = i->second;
				return true;
			}
			c = *p++;
			r = (c == ')');
			if (r)
			{
				r = Parse(SymbolType_Spacing, p, 0);
			}
			if (_memoize)
			{
				if (r)
					end[SymbolType_CLOSE][pBegin] = p;
				else
					fail[SymbolType_CLOSE].insert(pBegin);
			}
			return r;
		}

		case SymbolType_Char:
		{
			if (fail[SymbolType_Char].count(pBegin))
				return false;
			EndMap::iterator i = end[SymbolType_Char].find(pBegin);
			if (i != end[SymbolType_Char].end())
			{
				p = i->second;
				return true;
			}
			const char* b = p;
			c = *p++;
			r = (c == '\\');
			if (r)
			{
				const char* b2 = p;
				c = *p++;
				r = (c == 'n');
				if (!r)
				{
					p = b2;
					c = *p++;
					r = (c == 'r');
					if (!r)
					{
						p = b2;
						c = *p++;
						r = (c == 't');
						if (!r)
						{
							p = b2;
							c = *p++;
							r = (c == '\'');
							if (!r)
							{
								p = b2;
								c = *p++;
								r = (c == '\"');
								if (!r)
								{
									p = b2;
									c = *p++;
									r = (c == '[');
									if (!r)
									{
										p = b2;
										c = *p++;
										r = (c == ']');
										if (!r)
										{
											p = b2;
											c = *p++;
											r = (c == '\\');
											if (!r)
											{
												p = b2;
												c = *p++;
												r = (c >= '1' && c <= '9');
												if (r)
												{
													for (;;)
													{
														const char* b3 = p;
														c = *p++;
														r = (c >= '0' && c <= '9');
														if (!r)
														{
															p = b3;
															break;
														}
													}
													r = true;
												}
											}
										}
									}
								}
							}
						}
					}
				}
			}
			if (!r)
			{
				p = b;
				c = *p++;
				r = (c == '\\');
				r = !r;
				p = b;
				if (r)
				{
					c = *p++;
					r = (c != 0);
				}
			}
			if (_memoize)
			{
				if (r)
					end[SymbolType_Char][pBegin] = p;
				else
					fail[SymbolType_Char].insert(pBegin);
			}
			return r;
		}

		case SymbolType_Class:
		{
			if (fail[SymbolType_Class].count(pBegin))
				return false;
			EndMap::iterator i = end[SymbolType_Class].find(pBegin);
			if (i != end[SymbolType_Class].end())
			{
				p = i->second;
				return true;
			}
			c = *p++;
			r = (c == '[');
			if (r)
			{
				for (;;)
				{
					const char* b = p;
					c = *p++;
					r = (c == ']');
					r = !r;
					p = b;
					if (r)
					{
						r = Parse(SymbolType_Range, p, 1);
					}
					if (!r)
					{
						p = b;
						break;
					}
				}
				r = true;
				if (r)
				{
					c = *p++;
					r = (c == ']');
					if (r)
					{
						r = Parse(SymbolType_Spacing, p, 1);
					}
				}
			}
			if (_memoize)
			{
				if (r)
					end[SymbolType_Class][pBegin] = p;
				else
					fail[SymbolType_Class].insert(pBegin);
			}
			return r;
		}

		case SymbolType_Comment:
		{
			if (fail[SymbolType_Comment].count(pBegin))
				return false;
			EndMap::iterator i = end[SymbolType_Comment].find(pBegin);
			if (i != end[SymbolType_Comment].end())
			{
				p = i->second;
				return true;
			}
			c = *p++;
			r = (c == '#');
			if (r)
			{
				for (;;)
				{
					const char* b = p;
					r = Parse(SymbolType_EndOfLine, p, 0);
					r = !r;
					p = b;
					if (r)
					{
						c = *p++;
						r = (c != 0);
					}
					if (!r)
					{
						p = b;
						break;
					}
				}
				r = true;
				if (r)
				{
					r = Parse(SymbolType_EndOfLine, p, 0);
				}
			}
			if (_memoize)
			{
				if (r)
					end[SymbolType_Comment][pBegin] = p;
				else
					fail[SymbolType_Comment].insert(pBegin);
			}
			return r;
		}

		case SymbolType_DOT:
		{
			if (fail[SymbolType_DOT].count(pBegin))
				return false;
			EndMap::iterator i = end[SymbolType_DOT].find(pBegin);
			if (i != end[SymbolType_DOT].end())
			{
				p = i->second;
				return true;
			}
			c = *p++;
			r = (c == '.');
			if (r)
			{
				r = Parse(SymbolType_Spacing, p, 0);
			}
			if (_memoize)
			{
				if (r)
					end[SymbolType_DOT][pBegin] = p;
				else
					fail[SymbolType_DOT].insert(pBegin);
			}
			return r;
		}

		case SymbolType_Definition:
		{
			if (fail[SymbolType_Definition].count(pBegin))
				return false;
			EndMap::iterator i = end[SymbolType_Definition].find(pBegin);
			if (i != end[SymbolType_Definition].end())
			{
				p = i->second;
				return true;
			}
			r = Parse(SymbolType_Identifier, p, 1);
			if (r)
			{
				r = Parse(SymbolType_Spacing, p, 1);
				if (r)
				{
					r = Parse(SymbolType_LEFTARROW, p, 1);
					if (r)
					{
						r = Parse(SymbolType_Expression, p, 1);
					}
				}
			}
			if (_memoize)
			{
				if (r)
					end[SymbolType_Definition][pBegin] = p;
				else
					fail[SymbolType_Definition].insert(pBegin);
			}
			return r;
		}

		case SymbolType_EndOfFile:
		{
			if (fail[SymbolType_EndOfFile].count(pBegin))
				return false;
			EndMap::iterator i = end[SymbolType_EndOfFile].find(pBegin);
			if (i != end[SymbolType_EndOfFile].end())
			{
				p = i->second;
				return true;
			}
			const char* b = p;
			c = *p++;
			r = (c != 0);
			r = !r;
			p = b;
			if (_memoize)
			{
				if (r)
					end[SymbolType_EndOfFile][pBegin] = p;
				else
					fail[SymbolType_EndOfFile].insert(pBegin);
			}
			return r;
		}

		case SymbolType_EndOfLine:
		{
			if (fail[SymbolType_EndOfLine].count(pBegin))
				return false;
			EndMap::iterator i = end[SymbolType_EndOfLine].find(pBegin);
			if (i != end[SymbolType_EndOfLine].end())
			{
				p = i->second;
				return true;
			}
			const char* b = p;
			c = *p++;
			r = (c == '\r');
			if (r)
			{
				c = *p++;
				r = (c == '\n');
			}
			if (!r)
			{
				p = b;
				c = *p++;
				r = (c == '\n');
				if (!r)
				{
					p = b;
					c = *p++;
					r = (c == '\r');
				}
			}
			if (_memoize)
			{
				if (r)
					end[SymbolType_EndOfLine][pBegin] = p;
				else
					fail[SymbolType_EndOfLine].insert(pBegin);
			}
			return r;
		}

		case SymbolType_Expression:
		{
			if (fail[SymbolType_Expression].count(pBegin))
				return false;
			EndMap::iterator i = end[SymbolType_Expression].find(pBegin);
			if (i != end[SymbolType_Expression].end())
			{
				p = i->second;
				return true;
			}
			r = Parse(SymbolType_Sequence, p, 1);
			if (r)
			{
				for (;;)
				{
					const char* b = p;
					r = Parse(SymbolType_SLASH, p, 1);
					if (r)
					{
						r = Parse(SymbolType_Sequence, p, 1);
					}
					if (!r)
					{
						p = b;
						break;
					}
				}
				r = true;
			}
			if (_memoize)
			{
				if (r)
					end[SymbolType_Expression][pBegin] = p;
				else
					fail[SymbolType_Expression].insert(pBegin);
			}
			return r;
		}

		case SymbolType_Grammar:
		{
			if (fail[SymbolType_Grammar].count(pBegin))
				return false;
			EndMap::iterator i = end[SymbolType_Grammar].find(pBegin);
			if (i != end[SymbolType_Grammar].end())
			{
				p = i->second;
				return true;
			}
			r = Parse(SymbolType_Spacing, p, 1);
			if (r)
			{
				r = Parse(SymbolType_Definition, p, 1);
				if (r)
				{
					for (;;)
					{
						const char* b = p;
						r = Parse(SymbolType_Definition, p, 1);
						if (!r)
						{
							p = b;
							break;
						}
					}
					r = true;
					if (r)
					{
						r = Parse(SymbolType_EndOfFile, p, 1);
					}
				}
			}
			if (_memoize)
			{
				if (r)
					end[SymbolType_Grammar][pBegin] = p;
				else
					fail[SymbolType_Grammar].insert(pBegin);
			}
			return r;
		}

		case SymbolType_Identifier:
		{
			if (fail[SymbolType_Identifier].count(pBegin))
				return false;
			EndMap::iterator i = end[SymbolType_Identifier].find(pBegin);
			if (i != end[SymbolType_Identifier].end())
			{
				p = i->second;
				return true;
			}
			const char* b = p;
			c = *p++;
			r = (c >= 'a' && c <= 'z');
			if (!r)
			{
				p = b;
				c = *p++;
				r = (c >= 'A' && c <= 'Z');
				if (!r)
				{
					p = b;
					c = *p++;
					r = (c == '_');
				}
			}
			if (r)
			{
				for (;;)
				{
					const char* b2 = p;
					c = *p++;
					r = (c >= 'a' && c <= 'z');
					if (!r)
					{
						p = b2;
						c = *p++;
						r = (c >= 'A' && c <= 'Z');
						if (!r)
						{
							p = b2;
							c = *p++;
							r = (c >= '0' && c <= '9');
							if (!r)
							{
								p = b2;
								c = *p++;
								r = (c == '_');
							}
						}
					}
					if (!r)
					{
						p = b2;
						break;
					}
				}
				r = true;
			}
			if (_memoize)
			{
				if (r)
					end[SymbolType_Identifier][pBegin] = p;
				else
					fail[SymbolType_Identifier].insert(pBegin);
			}
			return r;
		}

		case SymbolType_LEFTARROW:
		{
			if (fail[SymbolType_LEFTARROW].count(pBegin))
				return false;
			EndMap::iterator i = end[SymbolType_LEFTARROW].find(pBegin);
			if (i != end[SymbolType_LEFTARROW].end())
			{
				p = i->second;
				return true;
			}
			c = *p++;
			r = (c == '<');
			if (r)
			{
				const char* b = p;
				c = *p++;
				r = (c == '-');
				if (!r)
				{
					p = b;
					c = *p++;
					r = (c == '=');
				}
				if (r)
				{
					r = Parse(SymbolType_Spacing, p, 1);
				}
			}
			if (_memoize)
			{
				if (r)
					end[SymbolType_LEFTARROW][pBegin] = p;
				else
					fail[SymbolType_LEFTARROW].insert(pBegin);
			}
			return r;
		}

		case SymbolType_Literal:
		{
			if (fail[SymbolType_Literal].count(pBegin))
				return false;
			EndMap::iterator i = end[SymbolType_Literal].find(pBegin);
			if (i != end[SymbolType_Literal].end())
			{
				p = i->second;
				return true;
			}
			const char* b = p;
			c = *p++;
			r = (c == '\'');
			if (r)
			{
				for (;;)
				{
					const char* b2 = p;
					c = *p++;
					r = (c == '\'');
					r = !r;
					p = b2;
					if (r)
					{
						r = Parse(SymbolType_Char, p, 1);
					}
					if (!r)
					{
						p = b2;
						break;
					}
				}
				r = true;
				if (r)
				{
					c = *p++;
					r = (c == '\'');
					if (r)
					{
						r = Parse(SymbolType_Spacing, p, 1);
					}
				}
			}
			if (!r)
			{
				p = b;
				c = *p++;
				r = (c == '\"');
				if (r)
				{
					for (;;)
					{
						const char* b3 = p;
						c = *p++;
						r = (c == '\"');
						r = !r;
						p = b3;
						if (r)
						{
							r = Parse(SymbolType_Char, p, 1);
						}
						if (!r)
						{
							p = b3;
							break;
						}
					}
					r = true;
					if (r)
					{
						c = *p++;
						r = (c == '\"');
						if (r)
						{
							r = Parse(SymbolType_Spacing, p, 1);
						}
					}
				}
			}
			if (_memoize)
			{
				if (r)
					end[SymbolType_Literal][pBegin] = p;
				else
					fail[SymbolType_Literal].insert(pBegin);
			}
			return r;
		}

		case SymbolType_NOT:
		{
			if (fail[SymbolType_NOT].count(pBegin))
				return false;
			EndMap::iterator i = end[SymbolType_NOT].find(pBegin);
			if (i != end[SymbolType_NOT].end())
			{
				p = i->second;
				return true;
			}
			c = *p++;
			r = (c == '!');
			if (r)
			{
				r = Parse(SymbolType_Spacing, p, 0);
			}
			if (_memoize)
			{
				if (r)
					end[SymbolType_NOT][pBegin] = p;
				else
					fail[SymbolType_NOT].insert(pBegin);
			}
			return r;
		}

		case SymbolType_OPEN:
		{
			if (fail[SymbolType_OPEN].count(pBegin))
				return false;
			EndMap::iterator i = end[SymbolType_OPEN].find(pBegin);
			if (i != end[SymbolType_OPEN].end())
			{
				p = i->second;
				return true;
			}
			c = *p++;
			r = (c == '(');
			if (r)
			{
				r = Parse(SymbolType_Spacing, p, 0);
			}
			if (_memoize)
			{
				if (r)
					end[SymbolType_OPEN][pBegin] = p;
				else
					fail[SymbolType_OPEN].insert(pBegin);
			}
			return r;
		}

		case SymbolType_PLUS:
		{
			if (fail[SymbolType_PLUS].count(pBegin))
				return false;
			EndMap::iterator i = end[SymbolType_PLUS].find(pBegin);
			if (i != end[SymbolType_PLUS].end())
			{
				p = i->second;
				return true;
			}
			c = *p++;
			r = (c == '+');
			if (r)
			{
				r = Parse(SymbolType_Spacing, p, 0);
			}
			if (_memoize)
			{
				if (r)
					end[SymbolType_PLUS][pBegin] = p;
				else
					fail[SymbolType_PLUS].insert(pBegin);
			}
			return r;
		}

		case SymbolType_Prefix:
		{
			if (fail[SymbolType_Prefix].count(pBegin))
				return false;
			EndMap::iterator i = end[SymbolType_Prefix].find(pBegin);
			if (i != end[SymbolType_Prefix].end())
			{
				p = i->second;
				return true;
			}
			const char* b = p;
			r = Parse(SymbolType_AND, p, 1);
			if (!r)
			{
				p = b;
				r = Parse(SymbolType_NOT, p, 1);
				if (!r)
				{
					p = b;
					r = true;
				}
			}
			if (r)
			{
				r = Parse(SymbolType_Suffix, p, 1);
			}
			if (_memoize)
			{
				if (r)
					end[SymbolType_Prefix][pBegin] = p;
				else
					fail[SymbolType_Prefix].insert(pBegin);
			}
			return r;
		}

		case SymbolType_Primary:
		{
			if (fail[SymbolType_Primary].count(pBegin))
				return false;
			EndMap::iterator i = end[SymbolType_Primary].find(pBegin);
			if (i != end[SymbolType_Primary].end())
			{
				p = i->second;
				return true;
			}
			const char* b = p;
			r = Parse(SymbolType_Identifier, p, 1);
			if (r)
			{
				r = Parse(SymbolType_Spacing, p, 1);
				if (r)
				{
					const char* b2 = p;
					r = Parse(SymbolType_LEFTARROW, p, 1);
					r = !r;
					p = b2;
				}
			}
			if (!r)
			{
				p = b;
				r = Parse(SymbolType_OPEN, p, 1);
				if (r)
				{
					r = Parse(SymbolType_Expression, p, 1);
					if (r)
					{
						r = Parse(SymbolType_CLOSE, p, 1);
					}
				}
				if (!r)
				{
					p = b;
					r = Parse(SymbolType_Literal, p, 1);
					if (!r)
					{
						p = b;
						r = Parse(SymbolType_Class, p, 1);
						if (!r)
						{
							p = b;
							r = Parse(SymbolType_DOT, p, 1);
						}
					}
				}
			}
			if (_memoize)
			{
				if (r)
					end[SymbolType_Primary][pBegin] = p;
				else
					fail[SymbolType_Primary].insert(pBegin);
			}
			return r;
		}

		case SymbolType_QUESTION:
		{
			if (fail[SymbolType_QUESTION].count(pBegin))
				return false;
			EndMap::iterator i = end[SymbolType_QUESTION].find(pBegin);
			if (i != end[SymbolType_QUESTION].end())
			{
				p = i->second;
				return true;
			}
			c = *p++;
			r = (c == '?');
			if (r)
			{
				r = Parse(SymbolType_Spacing, p, 0);
			}
			if (_memoize)
			{
				if (r)
					end[SymbolType_QUESTION][pBegin] = p;
				else
					fail[SymbolType_QUESTION].insert(pBegin);
			}
			return r;
		}

		case SymbolType_Range:
		{
			if (fail[SymbolType_Range].count(pBegin))
				return false;
			EndMap::iterator i = end[SymbolType_Range].find(pBegin);
			if (i != end[SymbolType_Range].end())
			{
				p = i->second;
				return true;
			}
			r = Parse(SymbolType_Char, p, 1);
			if (r)
			{
				const char* b = p;
				c = *p++;
				r = (c == '-');
				if (r)
				{
					r = Parse(SymbolType_Char, p, 1);
				}
				if (!r)
				{
					p = b;
					r = true;
				}
			}
			if (_memoize)
			{
				if (r)
					end[SymbolType_Range][pBegin] = p;
				else
					fail[SymbolType_Range].insert(pBegin);
			}
			return r;
		}

		case SymbolType_SLASH:
		{
			if (fail[SymbolType_SLASH].count(pBegin))
				return false;
			EndMap::iterator i = end[SymbolType_SLASH].find(pBegin);
			if (i != end[SymbolType_SLASH].end())
			{
				p = i->second;
				return true;
			}
			c = *p++;
			r = (c == '/');
			if (r)
			{
				r = Parse(SymbolType_Spacing, p, 0);
			}
			if (_memoize)
			{
				if (r)
					end[SymbolType_SLASH][pBegin] = p;
				else
					fail[SymbolType_SLASH].insert(pBegin);
			}
			return r;
		}

		case SymbolType_STAR:
		{
			if (fail[SymbolType_STAR].count(pBegin))
				return false;
			EndMap::iterator i = end[SymbolType_STAR].find(pBegin);
			if (i != end[SymbolType_STAR].end())
			{
				p = i->second;
				return true;
			}
			c = *p++;
			r = (c == '*');
			if (r)
			{
				r = Parse(SymbolType_Spacing, p, 0);
			}
			if (_memoize)
			{
				if (r)
					end[SymbolType_STAR][pBegin] = p;
				else
					fail[SymbolType_STAR].insert(pBegin);
			}
			return r;
		}

		case SymbolType_Sequence:
		{
			if (fail[SymbolType_Sequence].count(pBegin))
				return false;
			EndMap::iterator i = end[SymbolType_Sequence].find(pBegin);
			if (i != end[SymbolType_Sequence].end())
			{
				p = i->second;
				return true;
			}
			for (;;)
			{
				const char* b = p;
				r = Parse(SymbolType_Prefix, p, 1);
				if (!r)
				{
					p = b;
					break;
				}
			}
			r = true;
			if (_memoize)
			{
				if (r)
					end[SymbolType_Sequence][pBegin] = p;
				else
					fail[SymbolType_Sequence].insert(pBegin);
			}
			return r;
		}

		case SymbolType_Space:
		{
			if (fail[SymbolType_Space].count(pBegin))
				return false;
			EndMap::iterator i = end[SymbolType_Space].find(pBegin);
			if (i != end[SymbolType_Space].end())
			{
				p = i->second;
				return true;
			}
			const char* b = p;
			c = *p++;
			r = (c == ' ');
			if (!r)
			{
				p = b;
				c = *p++;
				r = (c == '\t');
				if (!r)
				{
					p = b;
					r = Parse(SymbolType_EndOfLine, p, 0);
				}
			}
			if (_memoize)
			{
				if (r)
					end[SymbolType_Space][pBegin] = p;
				else
					fail[SymbolType_Space].insert(pBegin);
			}
			return r;
		}

		case SymbolType_Spacing:
		{
			if (fail[SymbolType_Spacing].count(pBegin))
				return false;
			EndMap::iterator i = end[SymbolType_Spacing].find(pBegin);
			if (i != end[SymbolType_Spacing].end())
			{
				p = i->second;
				return true;
			}
			for (;;)
			{
				const char* b = p;
				r = Parse(SymbolType_Space, p, 0);
				if (!r)
				{
					p = b;
					r = Parse(SymbolType_Comment, p, 0);
				}
				if (!r)
				{
					p = b;
					break;
				}
			}
			r = true;
			if (_memoize)
			{
				if (r)
					end[SymbolType_Spacing][pBegin] = p;
				else
					fail[SymbolType_Spacing].insert(pBegin);
			}
			return r;
		}

		case SymbolType_Suffix:
		{
			if (fail[SymbolType_Suffix].count(pBegin))
				return false;
			EndMap::iterator i = end[SymbolType_Suffix].find(pBegin);
			if (i != end[SymbolType_Suffix].end())
			{
				p = i->second;
				return true;
			}
			r = Parse(SymbolType_Primary, p, 1);
			if (r)
			{
				const char* b = p;
				r = Parse(SymbolType_QUESTION, p, 1);
				if (!r)
				{
					p = b;
					r = Parse(SymbolType_STAR, p, 1);
					if (!r)
					{
						p = b;
						r = Parse(SymbolType_PLUS, p, 1);
						if (!r)
						{
							p = b;
							r = true;
						}
					}
				}
			}
			if (_memoize)
			{
				if (r)
					end[SymbolType_Suffix][pBegin] = p;
				else
					fail[SymbolType_Suffix].insert(pBegin);
			}
			return r;
		}

		default:
			assert(false);
			return false;
	}
}

bool PEGParser::Parser::Traverse(PEGParser::SymbolType _type, const char*& p, PEGParser::Symbols& v, bool _memoize)
{
	const char* pBegin = p;
	bool r = true;
	char c;
	switch (_type)
	{
		case SymbolType_Class:
		{
			if (fail[SymbolType_Class].count(pBegin))
				return false;
			c = *p++;
			r = (c == '[');
			if (r)
			{
				for (;;)
				{
					const char* b = p;
					size_t s = v.size();
					c = *p++;
					r = (c == ']');
					r = !r;
					p = b;
					if (r)
					{
						r = Visit(SymbolType_Range, p, v);
					}
					if (!r)
					{
						p = b;
						v.erase(v.begin() + s, v.end());
						break;
					}
				}
				r = true;
				if (r)
				{
					c = *p++;
					r = (c == ']');
					if (r)
					{
						r = Parse(SymbolType_Spacing, p, 1);
					}
				}
			}
			if (_memoize)
			{
				if (r)
					end[SymbolType_Class][pBegin] = p;
				else
					fail[SymbolType_Class].insert(pBegin);
			}
			return r;
		}

		case SymbolType_Definition:
		{
			if (fail[SymbolType_Definition].count(pBegin))
				return false;
			r = Visit(SymbolType_Identifier, p, v);
			if (r)
			{
				r = Parse(SymbolType_Spacing, p, 1);
				if (r)
				{
					r = Visit(SymbolType_LEFTARROW, p, v);
					if (r)
					{
						r = Visit(SymbolType_Expression, p, v);
					}
				}
			}
			if (_memoize)
			{
				if (r)
					end[SymbolType_Definition][pBegin] = p;
				else
					fail[SymbolType_Definition].insert(pBegin);
			}
			return r;
		}

		case SymbolType_Expression:
		{
			if (fail[SymbolType_Expression].count(pBegin))
				return false;
			r = Visit(SymbolType_Sequence, p, v);
			if (r)
			{
				for (;;)
				{
					const char* b = p;
					size_t s = v.size();
					r = Parse(SymbolType_SLASH, p, 1);
					if (r)
					{
						r = Visit(SymbolType_Sequence, p, v);
					}
					if (!r)
					{
						p = b;
						v.erase(v.begin() + s, v.end());
						break;
					}
				}
				r = true;
			}
			if (_memoize)
			{
				if (r)
					end[SymbolType_Expression][pBegin] = p;
				else
					fail[SymbolType_Expression].insert(pBegin);
			}
			return r;
		}

		case SymbolType_Grammar:
		{
			if (fail[SymbolType_Grammar].count(pBegin))
				return false;
			r = Parse(SymbolType_Spacing, p, 1);
			if (r)
			{
				r = Visit(SymbolType_Definition, p, v);
				if (r)
				{
					for (;;)
					{
						const char* b = p;
						size_t s = v.size();
						r = Visit(SymbolType_Definition, p, v);
						if (!r)
						{
							p = b;
							v.erase(v.begin() + s, v.end());
							break;
						}
					}
					r = true;
					if (r)
					{
						r = Parse(SymbolType_EndOfFile, p, 1);
					}
				}
			}
			if (_memoize)
			{
				if (r)
					end[SymbolType_Grammar][pBegin] = p;
				else
					fail[SymbolType_Grammar].insert(pBegin);
			}
			return r;
		}

		case SymbolType_Literal:
		{
			if (fail[SymbolType_Literal].count(pBegin))
				return false;
			const char* b = p;
			size_t s = v.size();
			c = *p++;
			r = (c == '\'');
			if (r)
			{
				for (;;)
				{
					const char* b2 = p;
					size_t s2 = v.size();
					c = *p++;
					r = (c == '\'');
					r = !r;
					p = b2;
					if (r)
					{
						r = Visit(SymbolType_Char, p, v);
					}
					if (!r)
					{
						p = b2;
						v.erase(v.begin() + s2, v.end());
						break;
					}
				}
				r = true;
				if (r)
				{
					c = *p++;
					r = (c == '\'');
					if (r)
					{
						r = Parse(SymbolType_Spacing, p, 1);
					}
				}
			}
			if (!r)
			{
				p = b;
				v.erase(v.begin() + s, v.end());
				c = *p++;
				r = (c == '\"');
				if (r)
				{
					for (;;)
					{
						const char* b3 = p;
						size_t s3 = v.size();
						c = *p++;
						r = (c == '\"');
						r = !r;
						p = b3;
						if (r)
						{
							r = Visit(SymbolType_Char, p, v);
						}
						if (!r)
						{
							p = b3;
							v.erase(v.begin() + s3, v.end());
							break;
						}
					}
					r = true;
					if (r)
					{
						c = *p++;
						r = (c == '\"');
						if (r)
						{
							r = Parse(SymbolType_Spacing, p, 1);
						}
					}
				}
			}
			if (_memoize)
			{
				if (r)
					end[SymbolType_Literal][pBegin] = p;
				else
					fail[SymbolType_Literal].insert(pBegin);
			}
			return r;
		}

		case SymbolType_Prefix:
		{
			if (fail[SymbolType_Prefix].count(pBegin))
				return false;
			const char* b = p;
			r = Parse(SymbolType_AND, p, 1);
			if (!r)
			{
				p = b;
				r = Parse(SymbolType_NOT, p, 1);
				if (!r)
				{
					p = b;
					r = true;
				}
			}
			if (r)
			{
				r = Visit(SymbolType_Suffix, p, v);
			}
			if (_memoize)
			{
				if (r)
					end[SymbolType_Prefix][pBegin] = p;
				else
					fail[SymbolType_Prefix].insert(pBegin);
			}
			return r;
		}

		case SymbolType_Primary:
		{
			if (fail[SymbolType_Primary].count(pBegin))
				return false;
			const char* b = p;
			size_t s = v.size();
			r = Visit(SymbolType_Identifier, p, v);
			if (r)
			{
				r = Parse(SymbolType_Spacing, p, 1);
				if (r)
				{
					const char* b2 = p;
					r = Parse(SymbolType_LEFTARROW, p, 1);
					r = !r;
					p = b2;
				}
			}
			if (!r)
			{
				p = b;
				v.erase(v.begin() + s, v.end());
				r = Parse(SymbolType_OPEN, p, 1);
				if (r)
				{
					r = Visit(SymbolType_Expression, p, v);
					if (r)
					{
						r = Parse(SymbolType_CLOSE, p, 1);
					}
				}
				if (!r)
				{
					p = b;
					v.erase(v.begin() + s, v.end());
					r = Visit(SymbolType_Literal, p, v);
					if (!r)
					{
						p = b;
						v.erase(v.begin() + s, v.end());
						r = Visit(SymbolType_Class, p, v);
						if (!r)
						{
							p = b;
							v.erase(v.begin() + s, v.end());
							r = Parse(SymbolType_DOT, p, 1);
						}
					}
				}
			}
			if (_memoize)
			{
				if (r)
					end[SymbolType_Primary][pBegin] = p;
				else
					fail[SymbolType_Primary].insert(pBegin);
			}
			return r;
		}

		case SymbolType_Range:
		{
			if (fail[SymbolType_Range].count(pBegin))
				return false;
			r = Visit(SymbolType_Char, p, v);
			if (r)
			{
				const char* b = p;
				size_t s = v.size();
				c = *p++;
				r = (c == '-');
				if (r)
				{
					r = Visit(SymbolType_Char, p, v);
				}
				if (!r)
				{
					p = b;
					v.erase(v.begin() + s, v.end());
					r = true;
				}
			}
			if (_memoize)
			{
				if (r)
					end[SymbolType_Range][pBegin] = p;
				else
					fail[SymbolType_Range].insert(pBegin);
			}
			return r;
		}

		case SymbolType_Sequence:
		{
			if (fail[SymbolType_Sequence].count(pBegin))
				return false;
			for (;;)
			{
				const char* b = p;
				size_t s = v.size();
				r = Visit(SymbolType_Prefix, p, v);
				if (!r)
				{
					p = b;
					v.erase(v.begin() + s, v.end());
					break;
				}
			}
			r = true;
			if (_memoize)
			{
				if (r)
					end[SymbolType_Sequence][pBegin] = p;
				else
					fail[SymbolType_Sequence].insert(pBegin);
			}
			return r;
		}

		case SymbolType_Suffix:
		{
			if (fail[SymbolType_Suffix].count(pBegin))
				return false;
			r = Visit(SymbolType_Primary, p, v);
			if (r)
			{
				const char* b = p;
				r = Parse(SymbolType_QUESTION, p, 1);
				if (!r)
				{
					p = b;
					r = Parse(SymbolType_STAR, p, 1);
					if (!r)
					{
						p = b;
						r = Parse(SymbolType_PLUS, p, 1);
						if (!r)
						{
							p = b;
							r = true;
						}
					}
				}
			}
			if (_memoize)
			{
				if (r)
					end[SymbolType_Suffix][pBegin] = p;
				else
					fail[SymbolType_Suffix].insert(pBegin);
			}
			return r;
		}

		case SymbolType_AND:
		case SymbolType_CLOSE:
		case SymbolType_Char:
		case SymbolType_Comment:
		case SymbolType_DOT:
		case SymbolType_EndOfFile:
		case SymbolType_EndOfLine:
		case SymbolType_Identifier:
		case SymbolType_LEFTARROW:
		case SymbolType_NOT:
		case SymbolType_OPEN:
		case SymbolType_PLUS:
		case SymbolType_QUESTION:
		case SymbolType_SLASH:
		case SymbolType_STAR:
		case SymbolType_Space:
		case SymbolType_Spacing:
			return Parse(_type, p);

		default:
			assert(false);
			return false;
	}
}

void PEGParser::Parser::Print(std::ostream& _os, PEGParser::SymbolType _type, const char* _pNode, int _tabs, int _maxLineSize)
{
	Symbols children;
	const char* pEnd = _pNode;
	Traverse(_type, pEnd, children);
	//std::cout << children.size() << "\n";
	if (!pEnd)
		throw std::runtime_error(str(boost::format("Parsing Failed for \"%1%\"") % SymbolName(_type)));

	int tabCount = _tabs;
	while (tabCount--)
		_os << "    ";

	_os << SymbolName(_type) << ": \"";

	size_t lineSize = 0;
	for (const char* p = _pNode; p != pEnd; ++p)
	{
		_os << EscapeChar(*p);
		if (++lineSize >= _maxLineSize)
		{
			_os << "...";
			break;
		}
	}

	_os << "\"\n";

	for (Symbols::iterator i = children.begin(), iEnd = children.end(); i != iEnd; ++i)
		Print(_os, i->first, i->second, _tabs + 1, _maxLineSize);
//*
	if (_tabs == 0)
	{
		_os << "Memo Count:\n";
		for (size_t k = 0; k < SymbolTypeCount; ++k)
		{
			_os << boost::format("%1% %|20t|End: %2% %|40t|Fail: %3%\n") % SymbolName(SymbolType(k)) % end[k].size() % fail[k].size();
		}
	}
//*/
}

bool PEGParser::Parser::Visit(PEGParser::SymbolType _type, const char*& _p, PEGParser::Symbols& _v)
{
	const char* pBegin = _p;
	bool r = Parse(_type, _p);
	if (r)
		_v.push_back(Symbol(_type, pBegin));
	return r;
}

PEGParser::Iterator::Iterator(PEGParser::SymbolType _type, const char* _pNode)
:	mType(_type)
,	mpNode(_pNode)
, mpSiblings(new Symbols)
, mpParser(new Parser)
{
	if (mpNode && End())
		mpSiblings->push_back(Symbol(mType, mpNode));

	miCurrent = mpSiblings->begin();				
}

PEGParser::Iterator::Iterator(const PEGParser::Iterator& _iOther)
: mType(_iOther.mType)
,	mpNode(_iOther.mpNode)
, mpSiblings(_iOther.mpSiblings)
, miCurrent(_iOther.miCurrent)
,	mpParser(_iOther.mpParser)
{
}

PEGParser::Iterator::operator bool() const
{
	return mpNode != 0;
}

PEGParser::Iterator& PEGParser::Iterator::operator++()
{
	mpChildren.reset();
	GoToNext(mType);
	return *this;
}

PEGParser::SymbolType PEGParser::Iterator::GetType() const
{
	return mType;
}

const char* PEGParser::Iterator::Begin() const
{
	assert(mpNode);
	return mpNode;
}

const char* PEGParser::Iterator::End() const
{
	assert(mpNode);
	const char* p = mpNode;
	mpParser->Parse(mType, p);
	return p;
}

PEGParser::Iterator PEGParser::Iterator::GetChild(PEGParser::SymbolType _childT) const
{
	assert(mpNode != 0);
	return Iterator(mpParser, GetChildren(), _childT);
}

PEGParser::Iterator PEGParser::Iterator::GetNext(PEGParser::SymbolType _childT) const
{
	assert(mpNode != 0);
	return Iterator(*this, _childT);
}

void PEGParser::Iterator::Print(std::ostream& _os, int _tabs, int _maxLineSize)
{
	mpParser->Print(_os, mType, mpNode, _tabs, _maxLineSize);
}

PEGParser::Iterator::Iterator(boost::shared_ptr<PEGParser::Parser> _pParser, boost::shared_ptr<PEGParser::Symbols> _pSiblings, PEGParser::SymbolType _childType)
: mType(_childType)
, mpSiblings(_pSiblings)
, miCurrent(_pSiblings->begin())
,	mpParser(_pParser)
{
	SkipSiblingsWithWrongType(_childType);
}

PEGParser::Iterator::Iterator(const PEGParser::Iterator& _iOther, PEGParser::SymbolType _childType)
: mType(_childType)
,	mpSiblings(_iOther.mpSiblings)
,	miCurrent(_iOther.miCurrent)
,	mpParser(_iOther.mpParser)
{
	SkipSiblingsWithWrongType(_childType);
}

void PEGParser::Iterator::GoToNext(PEGParser::SymbolType _childType)
{
	++miCurrent;
	SkipSiblingsWithWrongType(_childType);
}

void PEGParser::Iterator::SkipSiblingsWithWrongType(PEGParser::SymbolType _childType)
{
	Symbols::iterator iEnd = mpSiblings->end();
	while (miCurrent != iEnd && miCurrent->first != _childType)
		++miCurrent;
	mpNode = (miCurrent != iEnd) ? miCurrent->second : 0 ;
}

boost::shared_ptr<PEGParser::Symbols> PEGParser::Iterator::GetChildren() const
{
	if (!mpChildren)
	{
		Symbols children;
		const char* p = mpNode;
		mpParser->Traverse(mType, p, children);
		mpChildren.reset(new Symbols);
		mpChildren->swap(children);			
	}
	return mpChildren;
}

std::ostream& PEGParser::operator<<(std::ostream& _os, const PEGParser::Iterator& _i)
{
	if (_i)
	{
		for (const char *p = _i.Begin(), *pEnd = _i.End(); p != pEnd; ++p)
			_os.put(*p);
	}
	return _os;
}

