// This file was automatically generated by IPG on Tue Jul 19 00:51:58 2011
// (from /Users/alexk7/ipg/build/xcode/../../src/peg/peg.peg)
// DO NOT EDIT!
#include <cassert>
#include <stdexcept>
#include <ostream>
#include <sstream>
#include <iomanip>
#include <vector>
#include <boost/shared_ptr.hpp>
#include <boost/unordered_map.hpp>

namespace PEGParser
{
	enum SymbolType
	{
		SymbolType_AND = 1,
		SymbolType_CLOSE = 2,
		SymbolType_Char = 3,
		SymbolType_Class = 4,
		SymbolType_Comment = 5,
		SymbolType_DOT = 6,
		SymbolType_Definition = 7,
		SymbolType_EndOfFile = 8,
		SymbolType_EndOfLine = 9,
		SymbolType_Expression = 10,
		SymbolType_Grammar = 11,
		SymbolType_Identifier = 12,
		SymbolType_LEFTARROW = 13,
		SymbolType_Literal = 14,
		SymbolType_NOT = 15,
		SymbolType_OPEN = 16,
		SymbolType_PLUS = 17,
		SymbolType_Prefix = 18,
		SymbolType_Primary = 19,
		SymbolType_QUESTION = 20,
		SymbolType_Range = 21,
		SymbolType_SLASH = 22,
		SymbolType_STAR = 23,
		SymbolType_Sequence = 24,
		SymbolType_Space = 25,
		SymbolType_Spacing = 26,
		SymbolType_Suffix = 27,
	};

	typedef std::pair<SymbolType, const char*> Symbol;
	typedef std::vector<Symbol> Symbols;
	typedef boost::unordered_map<Symbol, const char*> MemoMap;

	const char* SymbolName(SymbolType _type);

	class Parser
	{
	public:
		const char* Parse(SymbolType _type, const char* _symbol);
		const char* Traverse(SymbolType _type, const char* _symbol, Symbols& _children);
		void Print(std::ostream& _os, SymbolType _type, const char* _pNode, int _tabs = 0, int _maxLineSize = 100);

	private:
		const char* Visit(SymbolType _type, const char* _p, Symbols& _v);
		MemoMap memoMap;
	};

	class Iterator
	{
	public:
		Iterator(SymbolType _type, const char* _pNode = 0);
		Iterator(const Iterator& _iOther);
		operator bool() const;
		Iterator& operator++();
		SymbolType GetType() const;
		const char* Begin() const;
		const char* End() const;
		Iterator GetChild(SymbolType _childT);
		Iterator GetNext(SymbolType _childT);
		void Print(std::ostream& _os, int _tabs = 0, int _maxLineSize = 100);

	private:
		Iterator(boost::shared_ptr<Parser> _pParser, boost::shared_ptr<Symbols> _pSiblings, SymbolType _childType);
		Iterator(const Iterator& _iOther, SymbolType _childType);
		void GoToNext(SymbolType _childType);
		void SkipSiblingsWithWrongType(SymbolType _childType);
		boost::shared_ptr<Symbols> GetChildren();

		SymbolType mType;
		const char* mpNode;
		boost::shared_ptr<Symbols> mpSiblings;
		Symbols::iterator miCurrent;
		boost::shared_ptr<Symbols> mpChildren;
		boost::shared_ptr<Parser> mpParser;
	};

	std::ostream& operator<<(std::ostream& _os, const Iterator& _i);
}

namespace
{
	typedef PEGParser::MemoMap::value_type Memo;
	typedef PEGParser::MemoMap::iterator MemoIterator;
	typedef std::pair<PEGParser::MemoMap::iterator, bool> MemoInsertResult;

	struct EscapeChar
	{
		EscapeChar(char _c);
		char c;
	};

	EscapeChar::EscapeChar(char _c) : c(_c)
	{
	}

	std::ostream& operator<<(std::ostream& _os, EscapeChar _e)
	{
		char c = _e.c;
		switch (c)
		{
			case '\\': c = '\\'; break;
			case '\n': c = 'n';  break;
			case '\r': c = 'r';  break;
			case '\t': c = 't';  break;
			case '\'': c = '\''; break;
			case '\"': c = '\"'; break;
			default:
				_os.put(c);
				return _os;
		}
		_os.put('\\');
		_os.put(c);
		return _os;
	}
}

const char* PEGParser::SymbolName(PEGParser::SymbolType _type)
{
	switch (_type)
	{
		case SymbolType_AND: return "AND";
		case SymbolType_CLOSE: return "CLOSE";
		case SymbolType_Char: return "Char";
		case SymbolType_Class: return "Class";
		case SymbolType_Comment: return "Comment";
		case SymbolType_DOT: return "DOT";
		case SymbolType_Definition: return "Definition";
		case SymbolType_EndOfFile: return "EndOfFile";
		case SymbolType_EndOfLine: return "EndOfLine";
		case SymbolType_Expression: return "Expression";
		case SymbolType_Grammar: return "Grammar";
		case SymbolType_Identifier: return "Identifier";
		case SymbolType_LEFTARROW: return "LEFTARROW";
		case SymbolType_Literal: return "Literal";
		case SymbolType_NOT: return "NOT";
		case SymbolType_OPEN: return "OPEN";
		case SymbolType_PLUS: return "PLUS";
		case SymbolType_Prefix: return "Prefix";
		case SymbolType_Primary: return "Primary";
		case SymbolType_QUESTION: return "QUESTION";
		case SymbolType_Range: return "Range";
		case SymbolType_SLASH: return "SLASH";
		case SymbolType_STAR: return "STAR";
		case SymbolType_Sequence: return "Sequence";
		case SymbolType_Space: return "Space";
		case SymbolType_Spacing: return "Spacing";
		case SymbolType_Suffix: return "Suffix";
	}
}

const char* PEGParser::Parser::Parse(SymbolType _type, const char* p)
{
	switch (_type)
	{
		case SymbolType_AND:
		{
			if (*p == '&') ++p; else p = 0;
			if (p)
			{
				p = Parse(SymbolType_Spacing, p);
			}
		  return p;
		}

		case SymbolType_CLOSE:
		{
			if (*p == ')') ++p; else p = 0;
			if (p)
			{
				p = Parse(SymbolType_Spacing, p);
			}
		  return p;
		}

		case SymbolType_Char:
		{
			MemoInsertResult r = memoMap.insert(Memo(Symbol(SymbolType_Char, p), 0));
			if (!r.second) return r.first->second;
			const char* b = p;
			if (*p == '\\') ++p; else p = 0;
			if (p)
			{
				const char* b2 = p;
				if (*p == 'n') ++p; else p = 0;
				if (!p)
				{
					p = b2;
					if (*p == 'r') ++p; else p = 0;
					if (!p)
					{
						p = b2;
						if (*p == 't') ++p; else p = 0;
						if (!p)
						{
							p = b2;
							if (*p == '\'') ++p; else p = 0;
							if (!p)
							{
								p = b2;
								if (*p == '\"') ++p; else p = 0;
								if (!p)
								{
									p = b2;
									if (*p == '[') ++p; else p = 0;
									if (!p)
									{
										p = b2;
										if (*p == ']') ++p; else p = 0;
										if (!p)
										{
											p = b2;
											if (*p == '\\') ++p; else p = 0;
											if (!p)
											{
												p = b2;
												if (*p >= '1' && *p <= '9') ++p; else p = 0;
												if (p)
												{
													for (;;)
													{
														const char* b3 = p;
														if (*p >= '0' && *p <= '9') ++p; else p = 0;
														if (!p)
														{
															p = b3;
															break;
														}
													}
												}
											}
										}
									}
								}
							}
						}
					}
				}
			}
			if (!p)
			{
				p = b;
				if (*p == '\\') ++p; else p = 0;
				if (p) p = 0; else p = b;
				if (p)
				{
					if (*p != 0) ++p; else p = 0;
				}
			}
			r.first->second = p;
		  return p;
		}

		case SymbolType_Class:
		{
			MemoInsertResult r = memoMap.insert(Memo(Symbol(SymbolType_Class, p), 0));
			if (!r.second) return r.first->second;
			if (*p == '[') ++p; else p = 0;
			if (p)
			{
				for (;;)
				{
					const char* b = p;
					if (*p == ']') ++p; else p = 0;
					if (p) p = 0; else p = b;
					if (p)
					{
						p = Parse(SymbolType_Range, p);
					}
					if (!p)
					{
						p = b;
						break;
					}
				}
				if (p)
				{
					if (*p == ']') ++p; else p = 0;
					if (p)
					{
						p = Parse(SymbolType_Spacing, p);
					}
				}
			}
			r.first->second = p;
		  return p;
		}

		case SymbolType_Comment:
		{
			if (*p == '#') ++p; else p = 0;
			if (p)
			{
				for (;;)
				{
					const char* b = p;
					p = Parse(SymbolType_EndOfLine, p);
					if (p) p = 0; else p = b;
					if (p)
					{
						if (*p != 0) ++p; else p = 0;
					}
					if (!p)
					{
						p = b;
						break;
					}
				}
				if (p)
				{
					p = Parse(SymbolType_EndOfLine, p);
				}
			}
		  return p;
		}

		case SymbolType_DOT:
		{
			if (*p == '.') ++p; else p = 0;
			if (p)
			{
				p = Parse(SymbolType_Spacing, p);
			}
		  return p;
		}

		case SymbolType_Definition:
		{
			MemoInsertResult r = memoMap.insert(Memo(Symbol(SymbolType_Definition, p), 0));
			if (!r.second) return r.first->second;
			p = Parse(SymbolType_Identifier, p);
			if (p)
			{
				p = Parse(SymbolType_Spacing, p);
				if (p)
				{
					p = Parse(SymbolType_LEFTARROW, p);
					if (p)
					{
						p = Parse(SymbolType_Expression, p);
					}
				}
			}
			r.first->second = p;
		  return p;
		}

		case SymbolType_EndOfFile:
		{
			const char* b = p;
			if (*p != 0) ++p; else p = 0;
			if (p) p = 0; else p = b;
		  return p;
		}

		case SymbolType_EndOfLine:
		{
			const char* b = p;
			if (*p == '\r') ++p; else p = 0;
			if (p)
			{
				if (*p == '\n') ++p; else p = 0;
			}
			if (!p)
			{
				p = b;
				if (*p == '\n') ++p; else p = 0;
				if (!p)
				{
					p = b;
					if (*p == '\r') ++p; else p = 0;
				}
			}
		  return p;
		}

		case SymbolType_Expression:
		{
			MemoInsertResult r = memoMap.insert(Memo(Symbol(SymbolType_Expression, p), 0));
			if (!r.second) return r.first->second;
			p = Parse(SymbolType_Sequence, p);
			if (p)
			{
				for (;;)
				{
					const char* b = p;
					p = Parse(SymbolType_SLASH, p);
					if (p)
					{
						p = Parse(SymbolType_Sequence, p);
					}
					if (!p)
					{
						p = b;
						break;
					}
				}
			}
			r.first->second = p;
		  return p;
		}

		case SymbolType_Grammar:
		{
			MemoInsertResult r = memoMap.insert(Memo(Symbol(SymbolType_Grammar, p), 0));
			if (!r.second) return r.first->second;
			p = Parse(SymbolType_Spacing, p);
			if (p)
			{
				p = Parse(SymbolType_Definition, p);
				if (p)
				{
					for (;;)
					{
						const char* b = p;
						p = Parse(SymbolType_Definition, p);
						if (!p)
						{
							p = b;
							break;
						}
					}
					if (p)
					{
						p = Parse(SymbolType_EndOfFile, p);
					}
				}
			}
			r.first->second = p;
		  return p;
		}

		case SymbolType_Identifier:
		{
			MemoInsertResult r = memoMap.insert(Memo(Symbol(SymbolType_Identifier, p), 0));
			if (!r.second) return r.first->second;
			const char* b = p;
			if (*p >= 'a' && *p <= 'z') ++p; else p = 0;
			if (!p)
			{
				p = b;
				if (*p >= 'A' && *p <= 'Z') ++p; else p = 0;
				if (!p)
				{
					p = b;
					if (*p == '_') ++p; else p = 0;
				}
			}
			if (p)
			{
				for (;;)
				{
					const char* b2 = p;
					if (*p >= 'a' && *p <= 'z') ++p; else p = 0;
					if (!p)
					{
						p = b2;
						if (*p >= 'A' && *p <= 'Z') ++p; else p = 0;
						if (!p)
						{
							p = b2;
							if (*p >= '0' && *p <= '9') ++p; else p = 0;
							if (!p)
							{
								p = b2;
								if (*p == '_') ++p; else p = 0;
							}
						}
					}
					if (!p)
					{
						p = b2;
						break;
					}
				}
			}
			r.first->second = p;
		  return p;
		}

		case SymbolType_LEFTARROW:
		{
			MemoInsertResult r = memoMap.insert(Memo(Symbol(SymbolType_LEFTARROW, p), 0));
			if (!r.second) return r.first->second;
			if (*p == '<') ++p; else p = 0;
			if (p)
			{
				const char* b = p;
				if (*p == '-') ++p; else p = 0;
				if (!p)
				{
					p = b;
					if (*p == '=') ++p; else p = 0;
					if (!p)
					{
						p = b;
						if (*p == '<') ++p; else p = 0;
					}
				}
				if (p)
				{
					p = Parse(SymbolType_Spacing, p);
				}
			}
			r.first->second = p;
		  return p;
		}

		case SymbolType_Literal:
		{
			MemoInsertResult r = memoMap.insert(Memo(Symbol(SymbolType_Literal, p), 0));
			if (!r.second) return r.first->second;
			const char* b = p;
			if (*p == '\'') ++p; else p = 0;
			if (p)
			{
				for (;;)
				{
					const char* b2 = p;
					if (*p == '\'') ++p; else p = 0;
					if (p) p = 0; else p = b2;
					if (p)
					{
						p = Parse(SymbolType_Char, p);
					}
					if (!p)
					{
						p = b2;
						break;
					}
				}
				if (p)
				{
					if (*p == '\'') ++p; else p = 0;
					if (p)
					{
						p = Parse(SymbolType_Spacing, p);
					}
				}
			}
			if (!p)
			{
				p = b;
				if (*p == '\"') ++p; else p = 0;
				if (p)
				{
					for (;;)
					{
						const char* b3 = p;
						if (*p == '\"') ++p; else p = 0;
						if (p) p = 0; else p = b3;
						if (p)
						{
							p = Parse(SymbolType_Char, p);
						}
						if (!p)
						{
							p = b3;
							break;
						}
					}
					if (p)
					{
						if (*p == '\"') ++p; else p = 0;
						if (p)
						{
							p = Parse(SymbolType_Spacing, p);
						}
					}
				}
			}
			r.first->second = p;
		  return p;
		}

		case SymbolType_NOT:
		{
			if (*p == '!') ++p; else p = 0;
			if (p)
			{
				p = Parse(SymbolType_Spacing, p);
			}
		  return p;
		}

		case SymbolType_OPEN:
		{
			if (*p == '(') ++p; else p = 0;
			if (p)
			{
				p = Parse(SymbolType_Spacing, p);
			}
		  return p;
		}

		case SymbolType_PLUS:
		{
			if (*p == '+') ++p; else p = 0;
			if (p)
			{
				p = Parse(SymbolType_Spacing, p);
			}
		  return p;
		}

		case SymbolType_Prefix:
		{
			MemoInsertResult r = memoMap.insert(Memo(Symbol(SymbolType_Prefix, p), 0));
			if (!r.second) return r.first->second;
			const char* b = p;
			p = Parse(SymbolType_AND, p);
			if (!p)
			{
				p = b;
				p = Parse(SymbolType_NOT, p);
				if (!p)
				{
					p = b;
				}
			}
			if (p)
			{
				p = Parse(SymbolType_Suffix, p);
			}
			r.first->second = p;
		  return p;
		}

		case SymbolType_Primary:
		{
			MemoInsertResult r = memoMap.insert(Memo(Symbol(SymbolType_Primary, p), 0));
			if (!r.second) return r.first->second;
			const char* b = p;
			p = Parse(SymbolType_Identifier, p);
			if (p)
			{
				p = Parse(SymbolType_Spacing, p);
				if (p)
				{
					const char* b2 = p;
					p = Parse(SymbolType_LEFTARROW, p);
					if (p) p = 0; else p = b2;
				}
			}
			if (!p)
			{
				p = b;
				p = Parse(SymbolType_OPEN, p);
				if (p)
				{
					p = Parse(SymbolType_Expression, p);
					if (p)
					{
						p = Parse(SymbolType_CLOSE, p);
					}
				}
				if (!p)
				{
					p = b;
					p = Parse(SymbolType_Literal, p);
					if (!p)
					{
						p = b;
						p = Parse(SymbolType_Class, p);
						if (!p)
						{
							p = b;
							p = Parse(SymbolType_DOT, p);
						}
					}
				}
			}
			r.first->second = p;
		  return p;
		}

		case SymbolType_QUESTION:
		{
			if (*p == '?') ++p; else p = 0;
			if (p)
			{
				p = Parse(SymbolType_Spacing, p);
			}
		  return p;
		}

		case SymbolType_Range:
		{
			MemoInsertResult r = memoMap.insert(Memo(Symbol(SymbolType_Range, p), 0));
			if (!r.second) return r.first->second;
			p = Parse(SymbolType_Char, p);
			if (p)
			{
				const char* b = p;
				if (*p == '-') ++p; else p = 0;
				if (p)
				{
					p = Parse(SymbolType_Char, p);
				}
				if (!p)
				{
					p = b;
				}
			}
			r.first->second = p;
		  return p;
		}

		case SymbolType_SLASH:
		{
			if (*p == '/') ++p; else p = 0;
			if (p)
			{
				p = Parse(SymbolType_Spacing, p);
			}
		  return p;
		}

		case SymbolType_STAR:
		{
			if (*p == '*') ++p; else p = 0;
			if (p)
			{
				p = Parse(SymbolType_Spacing, p);
			}
		  return p;
		}

		case SymbolType_Sequence:
		{
			MemoInsertResult r = memoMap.insert(Memo(Symbol(SymbolType_Sequence, p), 0));
			if (!r.second) return r.first->second;
			for (;;)
			{
				const char* b = p;
				p = Parse(SymbolType_Prefix, p);
				if (!p)
				{
					p = b;
					break;
				}
			}
			r.first->second = p;
		  return p;
		}

		case SymbolType_Space:
		{
			const char* b = p;
			if (*p == ' ') ++p; else p = 0;
			if (!p)
			{
				p = b;
				if (*p == '\t') ++p; else p = 0;
				if (!p)
				{
					p = b;
					p = Parse(SymbolType_EndOfLine, p);
				}
			}
		  return p;
		}

		case SymbolType_Spacing:
		{
			MemoInsertResult r = memoMap.insert(Memo(Symbol(SymbolType_Spacing, p), 0));
			if (!r.second) return r.first->second;
			for (;;)
			{
				const char* b = p;
				p = Parse(SymbolType_Space, p);
				if (!p)
				{
					p = b;
					p = Parse(SymbolType_Comment, p);
				}
				if (!p)
				{
					p = b;
					break;
				}
			}
			r.first->second = p;
		  return p;
		}

		case SymbolType_Suffix:
		{
			MemoInsertResult r = memoMap.insert(Memo(Symbol(SymbolType_Suffix, p), 0));
			if (!r.second) return r.first->second;
			p = Parse(SymbolType_Primary, p);
			if (p)
			{
				const char* b = p;
				p = Parse(SymbolType_QUESTION, p);
				if (!p)
				{
					p = b;
					p = Parse(SymbolType_STAR, p);
					if (!p)
					{
						p = b;
						p = Parse(SymbolType_PLUS, p);
						if (!p)
						{
							p = b;
						}
					}
				}
			}
			r.first->second = p;
		  return p;
		}

		default:
			assert(false);
			return 0;
	}
}

const char* PEGParser::Parser::Traverse(PEGParser::SymbolType _type, const char* p, PEGParser::Symbols& v)
{
	switch (_type)
	{
		case SymbolType_AND:
		{
			if (*p == '&') ++p; else p = 0;
			if (p)
			{
				p = Visit(SymbolType_Spacing, p, v);
			}
			return p;
		}

		case SymbolType_CLOSE:
		{
			if (*p == ')') ++p; else p = 0;
			if (p)
			{
				p = Visit(SymbolType_Spacing, p, v);
			}
			return p;
		}

		case SymbolType_Class:
		{
			MemoInsertResult r = memoMap.insert(Memo(Symbol(SymbolType_Class, p), 0));
			if (!r.second && !r.first->second) return 0;
			if (*p == '[') ++p; else p = 0;
			if (p)
			{
				for (;;)
				{
					const char* b = p;
					size_t s = v.size();
					if (*p == ']') ++p; else p = 0;
					if (p) p = 0; else p = b;
					if (p)
					{
						p = Visit(SymbolType_Range, p, v);
					}
					if (!p)
					{
						p = b;
						v.erase(v.begin() + s, v.end());
						break;
					}
				}
				if (p)
				{
					if (*p == ']') ++p; else p = 0;
					if (p)
					{
						p = Visit(SymbolType_Spacing, p, v);
					}
				}
			}
			r.first->second = p;
			return p;
		}

		case SymbolType_DOT:
		{
			if (*p == '.') ++p; else p = 0;
			if (p)
			{
				p = Visit(SymbolType_Spacing, p, v);
			}
			return p;
		}

		case SymbolType_Definition:
		{
			MemoInsertResult r = memoMap.insert(Memo(Symbol(SymbolType_Definition, p), 0));
			if (!r.second && !r.first->second) return 0;
			p = Visit(SymbolType_Identifier, p, v);
			if (p)
			{
				p = Visit(SymbolType_Spacing, p, v);
				if (p)
				{
					p = Visit(SymbolType_LEFTARROW, p, v);
					if (p)
					{
						p = Visit(SymbolType_Expression, p, v);
					}
				}
			}
			r.first->second = p;
			return p;
		}

		case SymbolType_Expression:
		{
			MemoInsertResult r = memoMap.insert(Memo(Symbol(SymbolType_Expression, p), 0));
			if (!r.second && !r.first->second) return 0;
			p = Visit(SymbolType_Sequence, p, v);
			if (p)
			{
				for (;;)
				{
					const char* b = p;
					size_t s = v.size();
					p = Traverse(SymbolType_SLASH, p, v);
					if (p)
					{
						p = Visit(SymbolType_Sequence, p, v);
					}
					if (!p)
					{
						p = b;
						v.erase(v.begin() + s, v.end());
						break;
					}
				}
			}
			r.first->second = p;
			return p;
		}

		case SymbolType_Grammar:
		{
			MemoInsertResult r = memoMap.insert(Memo(Symbol(SymbolType_Grammar, p), 0));
			if (!r.second && !r.first->second) return 0;
			p = Visit(SymbolType_Spacing, p, v);
			if (p)
			{
				p = Visit(SymbolType_Definition, p, v);
				if (p)
				{
					for (;;)
					{
						const char* b = p;
						size_t s = v.size();
						p = Visit(SymbolType_Definition, p, v);
						if (!p)
						{
							p = b;
							v.erase(v.begin() + s, v.end());
							break;
						}
					}
					if (p)
					{
						p = Parse(SymbolType_EndOfFile, p);
					}
				}
			}
			r.first->second = p;
			return p;
		}

		case SymbolType_LEFTARROW:
		{
			MemoInsertResult r = memoMap.insert(Memo(Symbol(SymbolType_LEFTARROW, p), 0));
			if (!r.second && !r.first->second) return 0;
			if (*p == '<') ++p; else p = 0;
			if (p)
			{
				const char* b = p;
				if (*p == '-') ++p; else p = 0;
				if (!p)
				{
					p = b;
					if (*p == '=') ++p; else p = 0;
					if (!p)
					{
						p = b;
						if (*p == '<') ++p; else p = 0;
					}
				}
				if (p)
				{
					p = Visit(SymbolType_Spacing, p, v);
				}
			}
			r.first->second = p;
			return p;
		}

		case SymbolType_Literal:
		{
			MemoInsertResult r = memoMap.insert(Memo(Symbol(SymbolType_Literal, p), 0));
			if (!r.second && !r.first->second) return 0;
			const char* b = p;
			size_t s = v.size();
			if (*p == '\'') ++p; else p = 0;
			if (p)
			{
				for (;;)
				{
					const char* b2 = p;
					size_t s2 = v.size();
					if (*p == '\'') ++p; else p = 0;
					if (p) p = 0; else p = b2;
					if (p)
					{
						p = Visit(SymbolType_Char, p, v);
					}
					if (!p)
					{
						p = b2;
						v.erase(v.begin() + s2, v.end());
						break;
					}
				}
				if (p)
				{
					if (*p == '\'') ++p; else p = 0;
					if (p)
					{
						p = Visit(SymbolType_Spacing, p, v);
					}
				}
			}
			if (!p)
			{
				p = b;
				v.erase(v.begin() + s, v.end());
				if (*p == '\"') ++p; else p = 0;
				if (p)
				{
					for (;;)
					{
						const char* b3 = p;
						size_t s3 = v.size();
						if (*p == '\"') ++p; else p = 0;
						if (p) p = 0; else p = b3;
						if (p)
						{
							p = Visit(SymbolType_Char, p, v);
						}
						if (!p)
						{
							p = b3;
							v.erase(v.begin() + s3, v.end());
							break;
						}
					}
					if (p)
					{
						if (*p == '\"') ++p; else p = 0;
						if (p)
						{
							p = Visit(SymbolType_Spacing, p, v);
						}
					}
				}
			}
			r.first->second = p;
			return p;
		}

		case SymbolType_NOT:
		{
			if (*p == '!') ++p; else p = 0;
			if (p)
			{
				p = Visit(SymbolType_Spacing, p, v);
			}
			return p;
		}

		case SymbolType_OPEN:
		{
			if (*p == '(') ++p; else p = 0;
			if (p)
			{
				p = Visit(SymbolType_Spacing, p, v);
			}
			return p;
		}

		case SymbolType_PLUS:
		{
			if (*p == '+') ++p; else p = 0;
			if (p)
			{
				p = Visit(SymbolType_Spacing, p, v);
			}
			return p;
		}

		case SymbolType_Prefix:
		{
			MemoInsertResult r = memoMap.insert(Memo(Symbol(SymbolType_Prefix, p), 0));
			if (!r.second && !r.first->second) return 0;
			const char* b = p;
			size_t s = v.size();
			p = Traverse(SymbolType_AND, p, v);
			if (!p)
			{
				p = b;
				v.erase(v.begin() + s, v.end());
				p = Traverse(SymbolType_NOT, p, v);
				if (!p)
				{
					p = b;
					v.erase(v.begin() + s, v.end());
				}
			}
			if (p)
			{
				p = Visit(SymbolType_Suffix, p, v);
			}
			r.first->second = p;
			return p;
		}

		case SymbolType_Primary:
		{
			MemoInsertResult r = memoMap.insert(Memo(Symbol(SymbolType_Primary, p), 0));
			if (!r.second && !r.first->second) return 0;
			const char* b = p;
			size_t s = v.size();
			p = Visit(SymbolType_Identifier, p, v);
			if (p)
			{
				p = Visit(SymbolType_Spacing, p, v);
				if (p)
				{
					const char* b2 = p;
					p = Parse(SymbolType_LEFTARROW, p);
					if (p) p = 0; else p = b2;
				}
			}
			if (!p)
			{
				p = b;
				v.erase(v.begin() + s, v.end());
				p = Traverse(SymbolType_OPEN, p, v);
				if (p)
				{
					p = Visit(SymbolType_Expression, p, v);
					if (p)
					{
						p = Traverse(SymbolType_CLOSE, p, v);
					}
				}
				if (!p)
				{
					p = b;
					v.erase(v.begin() + s, v.end());
					p = Visit(SymbolType_Literal, p, v);
					if (!p)
					{
						p = b;
						v.erase(v.begin() + s, v.end());
						p = Visit(SymbolType_Class, p, v);
						if (!p)
						{
							p = b;
							v.erase(v.begin() + s, v.end());
							p = Traverse(SymbolType_DOT, p, v);
						}
					}
				}
			}
			r.first->second = p;
			return p;
		}

		case SymbolType_QUESTION:
		{
			if (*p == '?') ++p; else p = 0;
			if (p)
			{
				p = Visit(SymbolType_Spacing, p, v);
			}
			return p;
		}

		case SymbolType_Range:
		{
			MemoInsertResult r = memoMap.insert(Memo(Symbol(SymbolType_Range, p), 0));
			if (!r.second && !r.first->second) return 0;
			p = Visit(SymbolType_Char, p, v);
			if (p)
			{
				const char* b = p;
				size_t s = v.size();
				if (*p == '-') ++p; else p = 0;
				if (p)
				{
					p = Visit(SymbolType_Char, p, v);
				}
				if (!p)
				{
					p = b;
					v.erase(v.begin() + s, v.end());
				}
			}
			r.first->second = p;
			return p;
		}

		case SymbolType_SLASH:
		{
			if (*p == '/') ++p; else p = 0;
			if (p)
			{
				p = Visit(SymbolType_Spacing, p, v);
			}
			return p;
		}

		case SymbolType_STAR:
		{
			if (*p == '*') ++p; else p = 0;
			if (p)
			{
				p = Visit(SymbolType_Spacing, p, v);
			}
			return p;
		}

		case SymbolType_Sequence:
		{
			MemoInsertResult r = memoMap.insert(Memo(Symbol(SymbolType_Sequence, p), 0));
			if (!r.second && !r.first->second) return 0;
			for (;;)
			{
				const char* b = p;
				size_t s = v.size();
				p = Visit(SymbolType_Prefix, p, v);
				if (!p)
				{
					p = b;
					v.erase(v.begin() + s, v.end());
					break;
				}
			}
			r.first->second = p;
			return p;
		}

		case SymbolType_Suffix:
		{
			MemoInsertResult r = memoMap.insert(Memo(Symbol(SymbolType_Suffix, p), 0));
			if (!r.second && !r.first->second) return 0;
			p = Visit(SymbolType_Primary, p, v);
			if (p)
			{
				const char* b = p;
				size_t s = v.size();
				p = Traverse(SymbolType_QUESTION, p, v);
				if (!p)
				{
					p = b;
					v.erase(v.begin() + s, v.end());
					p = Traverse(SymbolType_STAR, p, v);
					if (!p)
					{
						p = b;
						v.erase(v.begin() + s, v.end());
						p = Traverse(SymbolType_PLUS, p, v);
						if (!p)
						{
							p = b;
							v.erase(v.begin() + s, v.end());
						}
					}
				}
			}
			r.first->second = p;
			return p;
		}

		case SymbolType_Char:
		case SymbolType_Comment:
		case SymbolType_EndOfFile:
		case SymbolType_EndOfLine:
		case SymbolType_Identifier:
		case SymbolType_Space:
		case SymbolType_Spacing:
			return Parse(_type, p);

		default:
			assert(false);
			return 0;
	}
}

void PEGParser::Parser::Print(std::ostream& _os, PEGParser::SymbolType _type, const char* _pNode, int _tabs, int _maxLineSize)
{
	Symbols children;
	const char* pEnd = Traverse(_type, _pNode, children);
	if (!pEnd)
	{
		std::ostringstream oss;
		oss << "Parsing Failed for \"" << SymbolName(_type) << "\"";
		throw std::runtime_error(oss.str());
	}

	int tabCount = _tabs;
	while (tabCount--)
		_os << "    ";

	_os << SymbolName(_type) << ": \"";

	size_t lineSize = 0;
	for (const char* p = _pNode; p != pEnd; ++p)
	{
		_os << EscapeChar(*p);
		if (++lineSize >= _maxLineSize)
		{
			_os << "...";
			break;
		}
	}

	_os << "\"\n";

	for (Symbols::iterator i = children.begin(), iEnd = children.end(); i != iEnd; ++i)
		Print(_os, i->first, i->second, _tabs + 1, _maxLineSize);
	if (_tabs == 0)
	{
		size_t bucketCount = memoMap.bucket_count();
		size_t collisionCount = 0;
		for (size_t bucket = 0; bucket != bucketCount; ++bucket)
		{
			size_t bucketSize = memoMap.bucket_size(bucket);
			_os << "Bucket " << bucket << " Size: " << bucketSize << "\n";			
			if (bucketSize > 1)
					++collisionCount;
		}
		_os << "Max Load Factor: " << memoMap.max_load_factor() << "\n";
		_os << "Bucket Count: " << bucketCount << "\n";
		_os << "Collision Count: " << collisionCount << "\n";
		size_t nullCount = 0;
		size_t perSymbolCount[28] = {0};
		for (MemoMap::const_iterator j = memoMap.begin(), jEnd = memoMap.end(); j != jEnd; ++j)
		{
			++perSymbolCount[j->first.first];
			if (!j->second)
				++nullCount;
		}
		_os << "Null Count: " << nullCount << "\n";
		_os << "Memo Count: " << memoMap.size() << "\n";
		for (size_t k = 1; k <= 27; ++k)
			_os << SymbolName(SymbolType(k)) << " Count: " << perSymbolCount[k] << "\n";
	}
}

const char* PEGParser::Parser::Visit(PEGParser::SymbolType _type, const char* _p, PEGParser::Symbols& _v)
{
	const char* pEnd = Parse(_type, _p);
	if (pEnd)
		_v.push_back(Symbol(_type, _p));
	return pEnd;
}

PEGParser::Iterator::Iterator(PEGParser::SymbolType _type, const char* _pNode)
:	mType(_type)
,	mpNode(_pNode)
, mpSiblings(new Symbols)
, mpParser(new Parser)
{
	if (mpNode && End())
		mpSiblings->push_back(Symbol(mType, mpNode));

	miCurrent = mpSiblings->begin();				
}

PEGParser::Iterator::Iterator(const PEGParser::Iterator& _iOther)
: mType(_iOther.mType)
,	mpNode(_iOther.mpNode)
, mpSiblings(_iOther.mpSiblings)
, miCurrent(_iOther.miCurrent)
,	mpParser(_iOther.mpParser)
{
}

PEGParser::Iterator::operator bool() const
{
	return mpNode != 0;
}

PEGParser::Iterator& PEGParser::Iterator::operator++()
{
	mpChildren.reset();
	GoToNext(mType);
	return *this;
}

PEGParser::SymbolType PEGParser::Iterator::GetType() const
{
	return mType;
}

const char* PEGParser::Iterator::Begin() const
{
	assert(mpNode);
	return mpNode;
}

const char* PEGParser::Iterator::End() const
{
	assert(mpNode);
	if (mType == SymbolType(0))
		return mpNode + 1;
	return mpParser->Parse(mType, mpNode);
}

PEGParser::Iterator PEGParser::Iterator::GetChild(PEGParser::SymbolType _childT)
{
	assert(mpNode != 0);
	if (_childT == SymbolType(0))
		return Iterator(SymbolType(0), mpNode);
	else
		return Iterator(mpParser, GetChildren(), _childT);
}

PEGParser::Iterator PEGParser::Iterator::GetNext(PEGParser::SymbolType _childT)
{
	assert(mpNode != 0);
	if (_childT == SymbolType(0))
		return Iterator(SymbolType(0), mpNode + 1);
	else
		return Iterator(*this, _childT);
}

void PEGParser::Iterator::Print(std::ostream& _os, int _tabs, int _maxLineSize)
{
	mpParser->Print(_os, mType, mpNode, _tabs, _maxLineSize);
}

PEGParser::Iterator::Iterator(boost::shared_ptr<PEGParser::Parser> _pParser, boost::shared_ptr<PEGParser::Symbols> _pSiblings, PEGParser::SymbolType _childType)
: mType(_childType)
, mpSiblings(_pSiblings)
, miCurrent(_pSiblings->begin())
,	mpParser(_pParser)
{
	SkipSiblingsWithWrongType(_childType);
}

PEGParser::Iterator::Iterator(const PEGParser::Iterator& _iOther, PEGParser::SymbolType _childType)
: mType(_childType)
,	mpSiblings(_iOther.mpSiblings)
,	miCurrent(_iOther.miCurrent)
,	mpParser(_iOther.mpParser)
{
	SkipSiblingsWithWrongType(_childType);
}

void PEGParser::Iterator::GoToNext(PEGParser::SymbolType _childType)
{
	if (mType == SymbolType(0))
	{
		++mpNode;
		if (*mpNode == 0)
			mpNode = 0;
	}
	else
	{
		++miCurrent;
		SkipSiblingsWithWrongType(_childType);
	}
}

void PEGParser::Iterator::SkipSiblingsWithWrongType(PEGParser::SymbolType _childType)
{
	assert(_childType != SymbolType(0));
	Symbols::iterator iEnd = mpSiblings->end();
	while (miCurrent != iEnd && miCurrent->first != _childType)
		++miCurrent;
	mpNode = (miCurrent != iEnd) ? miCurrent->second : 0 ;
}

boost::shared_ptr<PEGParser::Symbols> PEGParser::Iterator::GetChildren()
{
	if (!mpChildren)
	{
		Symbols children;
		mpParser->Traverse(mType, mpNode, children);
		mpChildren.reset(new Symbols);
		mpChildren->swap(children);			
	}
	return mpChildren;
}

std::ostream& PEGParser::operator<<(std::ostream& _os, const PEGParser::Iterator& _i)
{
	if (_i)
	{
		for (const char *p = _i.Begin(), *pEnd = _i.End(); p != pEnd; ++p)
			_os.put(*p);
	}
	return _os;
}

